import { setCustomElementsManifest } from "@storybook/web-components";
const preview = { parameters: { options: { storySort: { order: ["Tokens", "Atoms", "Molecules"] } } } };
export default preview;
